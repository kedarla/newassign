<%= form_for(@food_event) do |f| %>
  <% if @food_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_event.errors.count, "error") %> prohibited this food_event from being saved:</h2>

      <ul>
      <% @food_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amar_present %><br>
    <%= f.check_box :amar_present %>
  </div>
  <div class="field">
    <%= f.label :akbar_present %><br>
    <%= f.check_box :akbar_present %>
  </div>
  <div class="field">
    <%= f.label :anthony_present %><br>
    <%= f.check_box :anthony_present %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
